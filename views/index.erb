<% @posts.each do |post| %>
    <article>
        <section class="text">
            <header>
                <h1><%= post.title %><%= edit_link 'post', post.id %></h1>
                <time datetime="<%= universal post.date %>"><%= date post.date %></time>
            </header>
            <%= textile post.text %>
        </section>
    </article>
<% end %>